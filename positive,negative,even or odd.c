#include <stdio.h>
void main()
{
int n;
printf("enter the value of n: ");
scanf("%d",&n);
if (n > 0)
{
printf("The number %d is positive and ", n);
}
else if (n< 0)
#include <stdio.h>
void main()
{
int n;
printf("enter the value of n: ");
scanf("%d",&n);
if (n > 0)
{
printf("The number %d is positive and ", n);
}
else if (n< 0)
